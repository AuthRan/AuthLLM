# CPU torch is plenty for ~14M-param inference on a free HF Space.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
gradio>=4.0
# Installs the ashugpt package (model + tokenizer code) straight from GitHub.
# Replace YOUR_GITHUB_USERNAME with your handle after you push the repo public.
git+https://github.com/YOUR_GITHUB_USERNAME/authLLM.git
