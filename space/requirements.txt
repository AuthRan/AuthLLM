# CPU torch is plenty for ~14M-param inference on a free HF Space.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
gradio>=4.0
# Installs the ashugpt package (model + tokenizer code) straight from GitHub.
# Installed straight from your public GitHub repo.
git+https://github.com/AuthRan/authLLM.git
