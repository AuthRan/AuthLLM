[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ashugpt"
version = "0.1.0"
description = "Educational, from-scratch GPT-style decoder-only LLM"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "pyyaml",
    "fastapi",
    "uvicorn",
]

[tool.setuptools.packages.find]
include = ["ashugpt*"]
